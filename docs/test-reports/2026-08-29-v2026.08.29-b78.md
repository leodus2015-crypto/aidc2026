# 测试报告 · 2026-08-29 · v2026.08.29 · build 78

- 站点版本：`v2026.08.29`（[`data/site-release.json`](../../data/site-release.json)）
- 资源版本：`78`（[`data/asset-version.json`](../../data/asset-version.json)）
- 生成：本机静态检查 + pytest；浏览器冒烟未在本轮逐条点完
- 给另一窗口：优先做下面「待优化」，做完再跑检查，不要扩 scope

## 结论

| 层 | 结果 |
|----|------|
| `python3 scripts/check-site.py` | **通过**（2 条 i18n lookup 提示，不失败） |
| `python3 -m pytest tests -q` | **15 passed**（0.19s） |
| 浏览器冒烟（[docs/TEST.md](../TEST.md)） | 未在本轮完整手测；入口页 HTTP 200 曾确认过 |

## 单测明细（均通过）

- `tests/test_api.py`：health ok/degraded、未知 key 404、DB down 503、PUT/analytics 无或错 Bearer → 401、`mask_ip`
- `tests/test_formulas.py`：标准 GQA KV、MLA < 标准、tokenMix、加权云价、TPS↔亿 token/日
- `tests/test_check_site.py`：`check-site.py` 退出码 0

## 待优化（建议下一窗口按序做）

### P1 · 清理多余 EN lookup（检查已提示）

`check-site.py` 对「EN 比 ZH 多出来的 `lookup.*`」只打印、不失败。这些键在中文包里已不存在，英文包里是死翻译。

**`i18n/ai-dc-deployment-perf.en.json`（29）** — 从 `lookup` 中删除下列键（整键，含 `lookup.` 前缀对应 JSON 里 `lookup` 对象的中文 key）：

- `12 × 智算柜 · 每 4 柜 1 实例，共 3 实例 · 上层 HCS+ModelArts（CPU 服务器池）弹性调度`
- `12 × 智算柜 · 每 4 柜 1 实例，共 3 实例 · 叠加 1 × AI 存储（SSD 池持久化 KV Cache）`
- `4 × 智算柜（AI服务器：HBM / DRAM / SSD）· 1 × AI 存储（SSD 池持久化 KV Cache）`
- `4智算柜 · 1AI存储`
- `4机大EP + AI存储`
- `4柜/实例 · 32卡 · 1AI存储`
- `64×NPU`
- `AI Coding`
- `AI 存储（通算柜 · OceanStor）`
- `AI 智算柜 ×12（4机大EP · 3 实例）`
- `CPU`
- `HCS+ModelArts`
- `HCS+ModelArts（CPU 服务器）`
- `ModelArts 弹性调度`
- `OceanStor · KV Cache 持久化池`
- `vs. 4机大EP 338M`
- `下降10×`
- `场景 {n}`
- `存算架构`
- `小时级–分钟级`
- `弹性推理调度 · CPU 服务器池`
- `弹性调度`
- `手机银行`
- `服务器`
- `模型启动时间`
- `白天推理 / 夜间训练`
- `负载均衡`
- `资源虚拟化`
- `风险控制`

**`i18n/aidc-investment-roi.en.json`（1）** — 删除 `lookup` 键：

- `DeepSeek-V4-Flash 公开价；各平台按官网/百炼等公开价目整理，单位元/百万 Token`

验收：`python3 scripts/check-site.py` 不再打印上述两文件的 lookup 提示。

### P2 · 基线仍未改（只做你窗口范围内的，勿一次全做）

摘自 [docs/REVIEW-BASELINE.md](../REVIEW-BASELINE.md)，自动化已覆盖的不再列。

| 优先级 | 位置 | 问题 | 建议改法 |
|--------|------|------|----------|
| 高 | `api/settings.py` | `ADMIN_TOKEN` 默认 `aidc2026` | 无环境变量时拒绝启动写接口，或强制从 `.env` 读取且无默认 |
| 高 | `data/config-seeds/site.unlock_password.json`、`datacenter-3d-*.html`、`js/aidc-investment-roi-page.js` | 解锁口令硬编码 `aidc2026` | 种子改为占位；前端无远程配置时禁用解锁或提示未配置 |
| 中 | `api/settings.py` `ALLOWED_CONFIG_KEYS` | `inference.defaults` / `aidc_calc.cfg` / `outline.2026` / `ai_usage` 无种子 | 补 `data/config-seeds/` 或从白名单去掉未用键 |
| 中 | `status.html` | 管理口令与 `ADMIN_TOKEN` 相同且路径可猜 | 与上两项一起改，避免再写死默认口令 |

### P3 · 本轮未做、优化窗口可抽查

浏览器（`./preview-8011.sh` → `http://127.0.0.1:8011/aidc/`）：

- `ai-dc-design.html` Tab / iframe / `?embed=1`
- `index.html` 混部、PD 分离、KV batch=0
- `aidc-investment-roi.html` 改参数、断 API 走本地、错误口令
- 中英 + Light/Dark；`status.html` 无口令无数据

## 不要动

- 不要改 [`docs/test-reports/`](.) 命名约定
- 不要为清 lookup 去改页面业务逻辑
- 3D 几何、Playwright、Jest 不在本报告范围
