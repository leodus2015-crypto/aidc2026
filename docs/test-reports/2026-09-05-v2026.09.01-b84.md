# 测试报告 · 2026-09-05 · v2026.09.01 · build 84

- 站点版本：`v2026.09.01`（[`data/site-release.json`](../../data/site-release.json)）
- 资源版本：`84`（[`data/asset-version.json`](../../data/asset-version.json)）
- 背景：全站整改 + 新页后，**重设计用例并从代码检视起测**
- 检视全文：[docs/REVIEW-BASELINE.md](../REVIEW-BASELINE.md)
- 冒烟清单：[docs/TEST.md](../TEST.md)

## 结论

| 层 | 结果 |
|----|------|
| 代码检视 | 已按新信息架构重写基线；重点是 TCP / 算力估算公式与规划 8 Tab 契约 |
| `python3 scripts/check-site.py` | **通过**（无 lookup 提示） |
| `python3 -m pytest tests -q` | **44 passed** |
| 浏览器 | 本机 `8011` 上入口与新 Tab/新页 HTTP **200**；未做完整手点四态 |

## 用例相对上一轮改了什么

1. **检视**：覆盖新增 `ai-dc-tcp.html`、`ai-dc-computeEst.html`，以及 `ai-dc-layout.html` 从跳转壳变为正式子页。
2. **静态**：`check-site.py` 增加规划导航契约——`VALID_TABS`、HTML `data-layout-tab` / `data-iframe-key`、`page-registry` embeds。
3. **单测**：TCP 1024 档日 Token `40640400000`、卡数 `1024`；渗透为 0 → Token 为 0；显存下限可绑定规划卡数；`tests/test_design_nav.py` 锁 Tab 集合。
4. **冒烟**：补 8 Tab 切换、`?tab=tcp|computeEst|plan` 直达、TCP 两档切换、算力页非法输入、机柜页本机可算。

## 单测覆盖（本轮新增）

- `test_tcp_1024_daily_tokens_matches_preset`
- `test_tcp_1024_compute_cards_matches_preset`
- `test_compute_est_zero_users_yields_zero_tokens`
- `test_hbm_floor_can_bind_planned_cards`
- `test_design_tabs_match_registry_and_markup`

既有 API（鉴权、409、verify、request-id）、KV/ROI、migrate/db 一并回归通过。

## 本机冒烟（HTTP）

```text
200  ai-dc-design.html
200  ai-dc-design.html?tab=tcp
200  ai-dc-design.html?tab=computeEst
200  ai-dc-tcp.html
200  ai-dc-computeEst.html
200  ai-dc-layout.html
200  index.html
200  aidc-investment-roi.html
200  status.html
```

未在浏览器里点：8 Tab 内容是否空白、TCP 1024/768 数字联动、算力页改参、中英×主题、embed。

## 待优化（给另一窗口，按序、勿扩 scope）

### P1 · 算力页与 TCP 默认口径写进注释

`ai-dc-computeEst.html` 的 `calculate()` 与 `ai-dc-tcp.html` 的 `COMMON`/`SC` 已有黄金用例。若改默认 DAU / 渗透 / `C_card` / `N`，必须同时改 `tests/test_formulas.py`，否则会红。

### P2 · 页内 I18N 与标准 bundle 并存

TCP / 算力 / 机柜仍用页内 `I18N` 数组。`?lang=en` 时抽查动态短语（规模名、机柜图例）是否仍中文。不要本轮抽函数重构整页。

### P3 · 手测缺口

按 [docs/TEST.md](../TEST.md) 新节走一遍：8 Tab、直达 URL、TCP 两档、算力非法输入、四态、embed。

## 不要动

- 不要为对齐用例去改 1024 档业务口径（除非产品要求）。
- 不要上 Playwright / Jest。
- 3D 几何、CSP、analytics 入库 IP 不在本报告范围。
