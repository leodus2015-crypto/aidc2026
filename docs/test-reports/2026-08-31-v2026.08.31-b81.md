# 测试报告 · 2026-08-31 · v2026.08.31 · build 81

- 站点版本：`v2026.08.31`（[`data/site-release.json`](../../data/site-release.json)）
- 资源版本：`81`（[`data/asset-version.json`](../../data/asset-version.json)）
- 生成：本机静态检查 + pytest + Headless Chrome CDP 按 [docs/TEST.md](../TEST.md) 浏览器冒烟
- 给另一窗口：本轮「待优化」已空；若上线请 commit → push → `./scripts/deploy.sh --sync-only`

## 结论

| 层 | 结果 |
|----|------|
| `python3 scripts/check-site.py` | **通过** |
| `python3 -m pytest tests -q` | **17 passed** |
| 浏览器冒烟（[docs/TEST.md](../TEST.md)） | **通过**（见下；修 1 处 embed） |
| 线上抽查（可选） | `ai-dc-design.html` / `lang-switch.js` / `common.zh.json` 均为 HTTP **200** |

## 本轮修复

- **`ai-dc-design.html` / `index.html`**：补上 `js/embed-mode.js` + `css/embed.css`。此前父壳 `?embed=1` 不加 `aidc-embed`，顶栏/页脚仍可见；子页已有 embed，仅父壳漏接。

## 浏览器冒烟明细

本地：`./preview-8011.sh` → `http://127.0.0.1:8011/aidc/`（资源 `?v=81`）。

### 入口与嵌套

- [x] `ai-dc-design.html` 默认 `panel-room-layout`，iframe `ai-dc-room-layout.html?embed=1&v=…`；二级 Tab 包装 `hidden` / `display:none`
- [x] `?tab=plan` → `panel-plan` + `ai-dc-layout_37.html`，页面非空白
- [x] `ai-dc-design.html?embed=1` / `index.html?embed=1`：`html.aidc-embed`，header/footer `display:none`、高度 0

### 推理计算

- [x] `index.html?tab=mixed`：`hbmUsed`/`hbmRemain` 有数（如 26.40 / 37.60），无 NaN；二级 Tab 隐藏
- [x] `?tab=separated`：切换 `pdDeployMode` 32→48，headline 与 used/remain 变化（含「瓶颈：Prefill」）
- [x] `?tab=kvcache`：合法 batch 有 GiB；`batch=0` → `--` +「batch_size 需为大于等于 1 的整数」，无 NaN

### ROI

- [x] 改 `computeP` 768→1536，总投资约翻倍（83,402.7 → 166,805.4 万元）
- [x] 配置来源文案：本地默认（数据库不可用或未配置）
- [x] 错误口令 →「密码错误，请重试。」，未进入已解锁状态

### 中英 / 主题

- [x] EN ↔ 中文切换无 `nav.*` key 泄漏；`?lang=en` 进入另一页语言保持 EN
- [x] Light → Dark → Light：`data-theme` 跟随

### 状态页

- [x] `status.html`：有口令框；未认证不展示分析数字

## 待优化

（无）
