# 测试报告 · 2026-09-01 · v2026.09.01 · build 84

- 站点版本：`v2026.09.01`（[`data/site-release.json`](../../data/site-release.json)）
- 资源版本：`84`（[`data/asset-version.json`](../../data/asset-version.json)）
- 生成：本机静态检查 + pytest；大导航同 Tab 再点行为已手测
- 给另一窗口：本轮功能已合入；部署后抽查线上 `?v=84`

## 结论

| 层 | 结果 |
|----|------|
| `python3 scripts/check-site.py` | **通过** |
| `python3 -m pytest tests -q` | **17 passed** |
| 浏览器冒烟（要点） | **通过**（见下） |

## 本版变更摘要

### Agentic 推理 · 模型与表单

- 混部下拉：移除 R1/V3.1；Pro 改为 `DeepSeek-V4-Pro-W4A8`（878 GB）；Flash 300 GB；Qwen 改为 `Qwen3.6-35B-A3B-W8A8`（39.8 GB）；GLM 改为 `GLM-5.2-W8A8`（774 GB）
- 「模型尺寸」改为 **「模型参数」**（284B / 1.6T / 35B / 744B），与 HBM 用的权重文件 GB 分离
- NPU 移除 Ascend A2，默认 Ascend A3
- PD 分离大模型仅保留 Flash + GLM-5.2

### 页面结构

- 「容量规划」「说明文档」仅在 PD混部 / PD分离 / KV Cache Tab 显示；推理原理 / 数据流不显示
- `index.html` / `ai-dc-design.html` 父壳补 `embed-mode.js` + `embed.css`

### 大导航

- 同页同 Tab 再点：切换/重算 + **`AidcRefreshFlash` 蓝色脉冲**（公共组件 `js/aidc-refresh-flash.js`）
- 推理原理 / 数据流：iframe 重载 + 脉冲；规划页各 Tab：iframe 重载 + 脉冲

## 部署后抽查（可选）

- [ ] `https://www.aidc2026.cn/index.html?tab=separated&v=84` HTTP 200
- [ ] `https://www.aidc2026.cn/js/aidc-refresh-flash.js?v=84` HTTP 200
- [ ] 大导航同 Tab 再点有脉冲反馈

## 待优化

（无）
